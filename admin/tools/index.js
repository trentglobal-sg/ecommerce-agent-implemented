// STUDENT FILE: this is the tool-registration hook used by EcommerceAgent.
// Import completed tools and return them from buildTools when ready.
const sharedTools = [];

function buildTools({ output }) {
  void output;
  return [];
}

module.exports = { sharedTools, buildTools };
